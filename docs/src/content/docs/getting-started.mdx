---
title: Getting started
description: Install flow_ui, wire the theme, and render your first conversation.
sidebar:
  order: 1
---

import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

Flow UI is an open-source Flutter UI library to build production-grade
Chat & AI assistant interfaces — a plain Flutter package, no codegen, no
transitive dependency tree. Its components draw the conversation state you
hand them and report what the user did through callbacks; everything
model-shaped stays in your app.

## Install

One line in your pubspec — the package depends on nothing but the Flutter
SDK:

```yaml title="pubspec.yaml"
dependencies:
  flow_ui: ^0.2.0
```

## Wire the theme

The design tokens — colors and typography — install as a `ThemeExtension`:

```dart title="main.dart"
MaterialApp(
  theme: ThemeData(extensions: [FlowTheme.light()]),
  darkTheme: ThemeData(
    brightness: Brightness.dark,
    extensions: [FlowTheme.dark()],
  ),
)
```

<Aside type="tip">
	Zero setup also works: when no `FlowTheme` is installed, components fall
	back to a preset matched to the ambient `ThemeData.brightness`.
</Aside>

Google Sans — the face the design system is set in — is fetched through
`google_fonts` on first use, so there is nothing to add to your own
`pubspec.yaml` fonts section.

## Render a conversation

Messages are plain data; `FlowThread` draws them:

```dart title="A three-role conversation"
FlowThread(
  messages: [
    FlowMessageData.text(
      id: '1',
      role: FlowMessageRole.user,
      text: 'What is flow_ui?',
    ),
    FlowMessageData.text(
      id: '2',
      role: FlowMessageRole.assistant,
      text: 'An open-source Flutter UI library to build production-grade Chat & AI assistant interfaces...',
    ),
    FlowMessageData.text(
      id: '3',
      role: FlowMessageRole.system,
      text: 'Model changed to Sonnet',
    ),
  ],
)
```

Streaming is data, not streams: as chunks arrive, rebuild with the updated
message list and a `status` of `FlowMessageStatus.streaming` — the thread
animates the reveal.

## Run the playground

Every component has a live stage in the hosted
[playground](https://flowui.stac.dev/playground) — variant pills and code
snippets included. It is also in the repo to run locally:

```bash
cd playground && flutter run -d chrome
```

For a minimal starting point, the repo's `example/` is the full chat screen
from the README as a single-file app: generate platform runners once with
`flutter create . --platforms=web`, then `flutter run -d chrome`.

## Next

<CardGrid>
	<LinkCard
		title="Theming"
		description="The token system: three levels of ink, translucent washes on opaque grounds, and how the M3 mapping works."
		href="/theming/"
	/>
	<LinkCard
		title="Message & Thread"
		description="Conversation rendering — roles, streaming, pending and error states."
		href="/components/message-thread/"
	/>
</CardGrid>
